# OmniTensors Environment Configuration
# Copy this file or fill in your SMTP credentials to receive live consultation emails.

# SMTP Configuration (e.g. Gmail, Outlook, SendGrid, Amazon SES)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# The inbox where consultation briefs will be delivered
RECIPIENT_EMAIL=kiran.kumar@omnitensors.in

# Server Port
PORT=3000
NODE_ENV=development
